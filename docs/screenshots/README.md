Drop 2 screenshot files here with these exact names (referenced by the root README.md):

- 01-homepage.png
- 02-sql-answer-chart.png

This file itself can be deleted once the real screenshots are in place.

---

**Separately:** `index.html` now references `/og-image.png` (Open Graph / Twitter
card image — what shows up when the live link is shared on LinkedIn, Slack,
Discord, etc.). That file needs to go in `public/og-image.png` at the project
root (not this folder), standard size **1200×630px**. Can reuse one of the
two screenshots above cropped to that ratio, or a purpose-made banner — just
needs to exist for the social preview to actually show anything instead of
a blank/broken image.
